---
title: 'Run a Pilot'
description: ''
---

<Steps>
<Step title="Collect data and format">
Run your survey, and store the necessary data for each participant's open-ended question they complete.
</Step>
<Step title="Call the API">

Call the API using our [endpoints](endpoints).

#### Example Usage

```python
import requests

url = "https://roundtable.ai/api/alias/latest"

payload = {
    "questions": {},
    "responses": {},
    "survey_id": "<string>",
    "participant_id": "<string>"
}
headers = {
    "api_key": "<string>",
    "Content-Type": "application/json"
}

response = requests.request("POST", url, json=payload, headers=headers)

print(response.text)
```
</Step>
</Steps>

